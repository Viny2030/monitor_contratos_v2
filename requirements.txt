# ─────────────────────────────────────────────────────────────
# requirements.txt — Monitor de Fenómenos Corruptivos v2
# Ph.D. Vicente Humberto Monteverde (2020)
#
# Instalación local (PyCharm):
#   pip install -r requirements.txt
#
# Verificar instalación:
#   python -c "import streamlit, pandas, plotly, bs4; print('OK')"
# ─────────────────────────────────────────────────────────────
# ── Web scraping ──────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0                  # parser HTML alternativo para bs4
urllib3>=2.0.0
# ── PDF (extracción de texto de avisos BORA en PDF) ───────────
pdfminer.six>=20221105
# ── Datos y análisis ──────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0              # lectura/escritura Excel (.xlsx)
# ── Dashboard ─────────────────────────────────────────────────
streamlit>=1.32.0
plotly>=5.18.0
# ── Scheduler (ejecución automática de diario.py) ─────────────
# Descomentar cuando implementes el scheduler:
# APScheduler>=3.10.0
# ── Calendario y feriados ─────────────────────────────────────
holidays>=0.46               # feriados nacionales Argentina (y otros países)
# ── Utilidades ────────────────────────────────────────────────
python-dateutil>=2.8.0       # parseo robusto de fechas
certifi>=2024.0.0            # certificados SSL
charset-normalizer>=3.0.0
# ── Base de datos ─────────────────────────────────────────────
psycopg2-binary>=2.9.0       # conexión PostgreSQL (Railway)
# ── Opcionales (para análisis avanzado futuro) ────────────────
# scikit-learn>=1.3.0        # clustering de organismos/proveedores
# networkx>=3.0              # grafos de relaciones CUIT-organismo
